package ar.fiuba.tdd.tp.games.cursedobject;

import ar.fiuba.tdd.tp.games.Character;
import ar.fiuba.tdd.tp.games.CharacterState;
import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * Created by swandelow on 4/27/16.
 */
public class CursedObjectTest {

    CursedObject target = new CursedObject();
/*
    @Test
    public void testPick() {
        Character character = new Character();
        assertEquals(CharacterState.HEALTHY, character.getState());
        assertEquals("Now you are cursed!", this.target.pick(character));
        assertEquals(CharacterState.CURSED, character.getState());
    }
    */
}
